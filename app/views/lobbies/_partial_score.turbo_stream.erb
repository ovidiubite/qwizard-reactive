<%= turbo_stream.update("game") do %>
  <%= render ScoreInBetween::Component.new(lobby: lobby, question_index: question_index, total_questions: total_questions, current_player_id: player_id) %>
<% end %>
